import React from "react";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { CheckCircle, Calendar, Mail, Clock } from "lucide-react";
import { Link } from "react-router-dom";
import { createPageUrl } from "@/utils";

export default function BookingConfirmation({ artist, bookingData }) {
  return (
    <div className="text-center space-y-8">
      <div className="flex justify-center">
        <div className="w-20 h-20 bg-green-100 rounded-full flex items-center justify-center">
          <CheckCircle className="w-12 h-12 text-green-600" />
        </div>
      </div>
      
      <div>
        <h2 className="text-3xl font-bold text-gray-900 mb-4">
          Booking Request Submitted!
        </h2>
        <p className="text-lg text-gray-600 max-w-2xl mx-auto">
          Your booking request has been sent to <strong>{artist.artist_name}</strong>. 
          You'll receive a confirmation email shortly, and the artist will respond to your request soon.
        </p>
      </div>

      <Card className="bg-blue-50 border-0">
        <CardContent className="p-8">
          <h3 className="text-xl font-semibold mb-6">What happens next?</h3>
          <div className="space-y-6">
            <div className="flex items-start gap-4">
              <div className="w-8 h-8 bg-blue-100 rounded-full flex items-center justify-center flex-shrink-0">
                <Mail className="w-4 h-4 text-blue-600" />
              </div>
              <div className="text-left">
                <h4 className="font-semibold">Email Confirmation</h4>
                <p className="text-sm text-gray-600">
                  You'll receive a confirmation email at {bookingData.client_email} with your booking details.
                </p>
              </div>
            </div>
            
            <div className="flex items-start gap-4">
              <div className="w-8 h-8 bg-blue-100 rounded-full flex items-center justify-center flex-shrink-0">
                <Clock className="w-4 h-4 text-blue-600" />
              </div>
              <div className="text-left">
                <h4 className="font-semibold">Artist Review</h4>
                <p className="text-sm text-gray-600">
                  {artist.artist_name} will review your request and respond within 24-48 hours.
                </p>
              </div>
            </div>
            
            <div className="flex items-start gap-4">
              <div className="w-8 h-8 bg-blue-100 rounded-full flex items-center justify-center flex-shrink-0">
                <Calendar className="w-4 h-4 text-blue-600" />
              </div>
              <div className="text-left">
                <h4 className="font-semibold">Booking Confirmation</h4>
                <p className="text-sm text-gray-600">
                  Once approved, you'll receive a detailed contract with all terms and conditions.
                </p>
              </div>
            </div>
          </div>
        </CardContent>
      </Card>

      <div className="flex flex-col sm:flex-row gap-4 justify-center">
        <Link to={createPageUrl(`ArtistPublicProfile?id=${artist.id}`)}>
          <Button variant="outline" className="px-8">
            Back to Artist Profile
          </Button>
        </Link>
        <Link to={createPageUrl("BrowseArtists")}>
          <Button className="bg-blue-600 hover:bg-blue-700 px-8">
            Browse More Artists
          </Button>
        </Link>
      </div>
    </div>
  );
}